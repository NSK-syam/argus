# --- implemented and exercised by the current code (backend/app/ml, tests) ---
# pandas/numpy/scikit-learn/xgboost are pinned EXACTLY (not >=) because
# backend/app/demo_bundle/ ships real pickled model artifacts -- a clean-
# machine verification pass found that an unpinned `scikit-learn>=1.4`
# resolves to whatever is newest on the day someone installs it, which
# raised InconsistentVersionWarning against the committed demo bundle
# (built with 1.8.0) the moment pip picked up 1.9.1. Pinning keeps a
# fresh `pip install` reproducing the exact environment the bundle and
# every documented benchmark number (test RMSE 18.42, etc.) were
# generated with.
pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
xgboost==3.2.0

# Fully pinned below too, for the same reason as pandas/numpy/scikit-learn/
# xgboost above: found in external code review that a `>=` range still
# lets a fresh install drift to whatever's newest that day. These exact
# versions are what the full test suite (58 passed) was last verified
# against; re-verify the suite before bumping any of them.
pytest==9.1.1
pydantic==2.13.3
anthropic==1.4.0
joblib==1.5.3
fastapi==0.141.1
uvicorn[standard]==0.46.0
sqlalchemy==2.0.52
mlflow==3.16.0
shap==0.51.0
httpx==0.28.1
python-multipart==0.0.26

# --- needed for the docker-compose Postgres deployment; sqlite is the
# default locally and in tests, so this isn't imported (or exercised by
# `pytest`) in this environment. Pinned to the version verified to
# install cleanly in a real Docker build (see backend/Dockerfile). ---
psycopg2-binary==2.9.13

# --- not yet wired into the code; Sep 18-23 items (bounded search
# already implemented by hand in app/ml/data/baseline.py). Pinned to the
# version verified to install cleanly in a real Docker build; neither is
# imported by any code path pytest exercises today. ---
langgraph==1.2.11
optuna==5.0.0
